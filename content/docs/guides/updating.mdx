---
title: Updating
description: Back up the database, update the image, and roll back if needed.
icon: RefreshCw
---

import { Step, Steps } from 'fumadocs-ui/components/steps';

Run all commands in the folder with your `docker-compose.yml` and `.env`. To move from 2.x to 3.0, read [Upgrading to 3.0](/guides/upgrading-to-3) first.

<Steps>

<Step>

### Back up the database

The update does not delete your data, but make a backup first:

```bash
mkdir -p backups
docker compose exec -T postgres sh -c 'pg_dump -U "$POSTGRES_USER" "$POSTGRES_DB"' > "backups/autotournament-$(date +%F-%H%M%S).sql"
```

The command reads the database user and name from the container, so it also works if you changed `DB_USER` or `DB_NAME`. If the stack is down, start the database first with `docker compose up -d postgres`.

</Step>

<Step>

### Pull the new image

```bash
docker compose pull
```

</Step>

<Step>

### Restart

```bash
docker compose up -d
```

`up -d` replaces the app container with the new image and keeps the database volume. Database changes run on their own when the app starts.

</Step>

<Step>

### Watch the log

```bash
docker compose logs -f auto-tournament
```

Wait until the log shows that the server started, then press `Ctrl+C`.

</Step>

<Step>

### Check that it works

```bash
curl -fsS http://localhost:3069/health
```

If the app does not start, the log usually shows the reason. The most common causes are a missing value in `.env` or a database that cannot be reached.

</Step>

</Steps>

## Use a fixed version

`latest` always pulls the newest release. To choose when you update, and to make it easy to go back, use a version number in `docker-compose.yml`:

```yaml title="docker-compose.yml"
services:
  auto-tournament:
    image: sivertio/auto-tournament:2.4.15
```

Versions are listed on the [releases page](https://github.com/Auto-Tournament/auto-tournament/releases). The image tag has no `v` in front.

## Go back to an older version

<Steps>

<Step>

### Set the older version

Set the older version in `image:` in `docker-compose.yml`, for example `sivertio/auto-tournament:2.4.13`.

</Step>

<Step>

### Pull and restart

```bash
docker compose pull
docker compose up -d
```

</Step>

</Steps>

A newer version can change the database. If the older version does not start after that, restore the backup you made before the update into an empty database.

## After you update

- If you lost admin access, set `ADMIN_STEAM_IDS`. See [First login](/getting-started/first-login#if-you-are-not-admin).
- Update Auto Tournament CS2 on your servers too, so all servers run the same version. See [Update the CS2 plugin](/cs2/plugin/install#update), or run `sudo csm update-plugins` with CS2 Server Manager.
- To free disk space from old images:

```bash
docker image prune -f
```
